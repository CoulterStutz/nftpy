name: Check NFTpy/opensea.py for Errors

on:
  push:
    paths:
      - 'NFTpy/opensea.py'
  pull_request:
    paths:
      - 'NFTpy/opensea.py'

jobs:
  check-opensea:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'

    - name: Install Poetry
      run: curl -sSL https://install.python-poetry.org | python3 -

    - name: Install dependencies
      run: poetry install

    - name: Check for errors
      run: poetry run python -m compileall NFTpy/opensea.py
